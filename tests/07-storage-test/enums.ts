export enum Operators {
  test_operator = "test_operator",
  coingaming = "coingaming",
  ygf = "ygf",
}

export enum searchOptions {
  userName = "Search by User Name",
  id = "Search by User ID",
  email = "Search by email or %email%",
}
